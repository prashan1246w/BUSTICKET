Django==5.0.6                # Django framework
khalti==1.0.0                # Khalti payment integration
Pillow==10.3.0               # For handling image uploads (profile pictures)
python-dotenv==1.0.1         # For managing environment variables (optional)
gunicorn==21.2.0             # For deploying the app (optional)
whitenoise==6.6.0            # For serving static files in production (optional)
psycopg2-binary==2.9.9       # For PostgreSQL database (optional, if using PostgreSQL)